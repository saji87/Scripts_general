set -e

for d in */ ; do
    if test -f "$d/makefile"; then
        echo
        echo "----- Building $d -----"
        echo
        pushd "$d" > /dev/null
        make
        popd > /dev/null
    fi
done